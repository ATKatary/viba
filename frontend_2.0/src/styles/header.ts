import { SxProps } from "@mui/material";

class HeaderStyleSheet {
    public static title: SxProps = {
        fontWeight: "bold",
        fontSize: "var(--font-size-title)",
        color: "var(--text-color-primary)",
    }
}

export default HeaderStyleSheet;